import React from 'react';
import './Player.css';

function Player({ spotify }) {
  return (
    <div>
      {/* Sidebar */}
      {/* Body */}
      {/* Footer */}
    </div>
  );
}

export default Player;
